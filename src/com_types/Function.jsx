import React from 'react'

const Function = (props) => {
  return (
    <div>
      <h1>hello {props.name}</h1>
    </div>
  )
}

export default Function

//stateless component - it doesnt store state(data), it display only data
//props(properties) - 